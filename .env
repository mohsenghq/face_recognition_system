# --- System Configuration ---

# The source for the video stream.
# Can be a webcam index (e.g., "0"), a video file path (e.g., "/path/to/video.mp4"),
# or a network stream URL (e.g., "rtsp://...").
# Note: If using a local file path, you may need to mount it as a volume in docker-compose.yml.
VIDEO_SOURCE="./test.mp4"

# The desired frames per second to process from the video source.
FPS=10

# Note: Service hostnames (RABBITMQ_HOST, MILVUS_HOST) are automatically
# handled by Docker Compose and generally do not need to be set here.
